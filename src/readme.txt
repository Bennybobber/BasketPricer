How to run:

Prerequisites:

Have Node installed.

Run the following commands:

CD into ./src

npm install

npx tsc BasketPriceExample.ts

node BasketPriceExample.js

(It may complain about needing to change your target library but it still compiled for me and ran despite
specifying the version as being es2019).